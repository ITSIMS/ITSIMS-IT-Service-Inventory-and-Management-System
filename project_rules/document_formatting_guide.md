# Руководство по форматированию документов ITSIMS

| Дата       | Номер версии | Автор       | Ссылка на задачу                                                                   | Комментарий               |
| ---------- | ------------ | ----------- | ---------------------------------------------------------------------------------- | ------------------------- |
| 2025-12-04 | v1.0         | Когановский | [2025-11-29_Koganovsky.md](../task-tracker/2_in_progress/2025-11-29_Koganovsky.md) | Первая версия руководства |

---

## 1. Цель документа

Данное руководство определяет единые стандарты форматирования документации проекта ITSIMS. Цель — обеспечить читаемость, единообразие и простоту поддержки всех документов проекта.

**Принципы:**
- **Простота** — минимум формализма, максимум пользы
- **Единообразие** — один стиль для всех типов документов
- **Git-friendly** — форматы, удобные для версионирования
- **Автоматизация** — возможность генерации PDF при необходимости

---

## 2. Выбор формата

### 2.1 Markdown — основной формат

**Все документы проекта создаются в формате Markdown (`.md`).**

**Почему Markdown:**
- ✅ Читаем в исходном виде (открывается в любом редакторе)
- ✅ Понятные изменения в Git (текстовый diff)
- ✅ Простота редактирования (не нужны специальные программы)
- ✅ Легко конвертировать в PDF через Pandoc
- ✅ Поддержка таблиц, списков, кода, ссылок

**Когда использовать:**
- По умолчанию для любого текстового документа проекта.

### 2.2 LaTeX — для исключительных случаев

**LaTeX используется только когда Markdown не справляется.**

**Когда можно использовать LaTeX:**
- Сложные математические формулы (редко нужно в нашем проекте)
- Специфичное оформление с точным контролем вёрстки
- Требования заказчика к формату документа

**⚠️ Важно:** Перед использованием LaTeX нужно:
1. Убедиться, что Markdown и Pandoc не справляется с задачей
2. Обсудить необходимость с командой
3. Указать причину выбора LaTeX в истории версий документа

---

## 3. Правила форматирования текста

### 3.1 Выделение текста

**Полужирный (`**текст**`):**
- Определения и термины при первом упоминании
- Заголовки требований: **Требование (F1):**
- Ключевые моменты, на которые нужно обратить внимание
- Статусы: **Утверждено**, **В работе**, **Отклонено**

**Курсив (`_текст_`):**
- Названия документов: _Техническое задание_, _План конфигурационного управления_
- Названия систем и компонентов: _ITSIMS_, _API Gateway_
- Акцент на конкретное слово в предложении

**Моноширинный (`` `текст` ``):**
- Имена файлов и путей: `artifacts/1_technical.md`
- Названия команд: `git commit`, `pandoc`
- Переменные и идентификаторы: `userId`, `serviceId`
- Фрагменты кода: `return true;`
- Префиксы требований в тексте: требование `F1` описывает...

**Комбинации:**
- Допускается **полужирный + моноширинный** для важных команд: **`git push`**
- Не используются **жирный курсив** или другие сложные комбинации

### 3.2 Заголовки

**Иерархия:**
- `# H1` — только название документа (один раз в начале)
- `## H2` — основные разделы документа
- `### H3` — подразделы
- `#### H4` — если необходимо дополнительное дробление (редко)

**Правила:**
- Заголовки пишутся без точки в конце
- Между заголовком и текстом — одна пустая строка
- Между разделами — три дефиса `---` на отдельной строке

**Пример:**
```markdown
## 2. Архитектура системы

Описание архитектуры...

### 2.1 Компоненты

Описание компонентов...

---

## 3. Требования
```

### 3.3 Списки

**Маркированные списки:**
- Используются для неупорядоченных элементов
- Символ маркера: `-` (дефис)
- Вложенность через 2 пробела

```markdown
- Уровень 1
  - Уровень 2
    - Уровень 3
```

**Нумерованные списки:**
- Используются для последовательных шагов, приоритетов
- Формат: `1.`, `2.`, `3.` (автоматическая нумерация не используется)
- Вложенность аналогична маркированным

**Чеклисты:**
- Используются для отслеживания выполнения задач
- Формат: `- [ ]` (не выполнено), `- [x]` (выполнено)

```markdown
- [ ] Задача не выполнена
- [x] Задача выполнена
```

### 3.4 Таблицы

**Выравнивание:**
- Заголовки — по левому краю
- Текстовые столбцы — по левому краю
- Числовые столбцы — по правому краю
- Столбцы с датами — по центру

**Синтаксис выравнивания:**
```markdown
| Левый    | Центр    | Правый   |
| -------- | :------: | -------: |
| текст    | текст    | 100      |
```

**Обязательные элементы:**
- Все таблицы имеют заголовки
- Версионная таблица — в начале каждого документа (см. раздел 4)
- Таблицы требований — с идентификаторами

### 3.5 Ссылки

**Внутренние ссылки (якоря):**
```markdown
[Перейти к разделу](#3-правила-форматирования-текста)
```
- Якорь генерируется автоматически из заголовка
- Пробелы заменяются на дефисы
- Все буквы в нижнем регистре

**Ссылки на файлы:**
- Относительные пути: `[Задача](../task-tracker/1_sprint/2025-11-29_Koganovsky.md)`
- Для ссылок внутри проекта всегда используются относительные пути

**Внешние ссылки:**
```markdown
[Документация Pandoc](https://pandoc.org/MANUAL.html)
```

### 3.6 Блоки кода

**Инлайн код:**
- Используется для коротких фрагментов: `git commit`

**Блоки кода:**
````markdown
```bash
git add file.md
git commit -m "feat: добавить документ"
```
````

**Обязательно указывать язык:**
- `bash` — для команд терминала
- `markdown` — для примеров Markdown
- `python`, `javascript`, `json` — для соответствующего кода
- `plaintext` — если язык не определён

### 3.7 Цитаты и примечания

**Цитаты:**
```markdown
> Это цитата или важное примечание
```

**Предупреждения и заметки:**
```markdown
> ⚠️ **Важно:** Критическая информация

> 💡 **Примечание:** Дополнительная информация
```

---


